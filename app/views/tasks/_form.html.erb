<%= simple_form_for @task do |f| %>
	<%= f.input :title, label: 'Title', error: 'Title is required' %>
	<%= f.input :description, label: 'Description' %>
	<%= f.input :address, label: 'Address (optional | example: 613 Broadway E, Seattle, WA 98102)' %>
	<%= f.button :submit %>
<% end %>
